# NIPON26 — מדריך לסשן חדש

טיול של יובל ושיר ליפן, 14.10–25.11.2026, 42 לילות. הריפו הוא גם אתר התכנון וגם
האפליקציה שנושאים בטיול. **עברית, RTL, בכל הממשק.**

יובל מדבר עברית. תענה בעברית.

---

## שני ענפים, שני דברים

| ענף | מה זה |
|---|---|
| `main` | אתר התכנון. זה מה ש-GitHub Pages הגיש עד כה. |
| `App` | **כאן עובדים.** ה-PWA — אותם נתונים, מסכים חדשים, אופליין, ניתן להתקנה. |

כרגע Pages מגיש את `App`. `main` ממשיך לקבל עדכוני תוכן מסשנים אחרים —
**למזג `main` פנימה מדי כמה ימים.**

## המקור היחיד לאמת: `data.js`

קובץ אחד, `window.TRIP = {...}`, ~180KB. כל מה שמוצג בכל מסך נגזר ממנו בזמן ריצה.
אין תוכן טיול קשיח בשום מקום אחר. המבנה:

- `days[]` — 45 ימים. `t` הוא `"30.10 — נקאנויו → נגיסו (Nagiso · 南木曽) · 🟡 ..."`.
  **התאריך נגזר מפרסור הכותרת הזו** (`today.js`, `app.js`). `acts[]` = פעילויות עם
  `ic` (אימוג׳י), `t`, `d`, `cr` (עומק קהל 🟢/🟡/🔴), `l[]` (לינקים).
- `phases[]` — 4 שלבים. `dayPhase` ממפה `day.st` → **אינדקס במערך**. מחיקת שלב
  מזיזה את כל האינדקסים שאחריו — צריך למפות מחדש גם `dayPhase`, גם `mapPoints[].ph`
  וגם `ph` של פריטי החלטות. `check.js` תופס את זה.
- `dish{}` — מנה לכל אזור, ממופה לפי תחילת `day.st` (אותו שדה נקי כמו `dayPhase`).
- `budget.booked[]` — לינות. `d` הוא טווח כמו `"27–30.10"`.
- `decisions[]` — קבוצות עם `items[]`. `open:true` = פתוח עכשיו · `todo:true` = משימה
  ולא החלטה · `res` = מה נסגר בפועל · `cut:true` = בוטל, לא מוצג · `decided` = אינדקס שנבחר.
- `docs[]`, `mapPoints[]` (עם `lat`/`lng`), `resources[]`.

**כתובות יפניות וטלפונים חיים בתוך טקסט ההחלטות**, לא בשדה נפרד. `App.factsFor(name)`
ב-`app.js` שולף אותם ברג׳קס. זה שביר בכוונה — עדיף על הקלדה כפולה שתתפצל מהמקור.

## שכבת האפליקציה (ענף `App`)

```
app.css   ערכות יום/לילה כמשתני CSS, כרטיסים, ניווט, שכבות הסצנה
app.js    App.boot() — סצנה, ניווט, ערכה, מזג אוויר, פארלקס. עוזרים משותפים.
art.js    ציורי SVG (חתולות, פנסים, דמויות) — עכשיו בעיקר גיבוי לתמונות
today.html + today.js     **מסך הפתיחה של ה-PWA.** מה עכשיו ומה אחר כך —
                          כל הפעילויות, המנה, הלינה. הסיבה שהאפליקציה קיימת.
itinerary.html + .js      שלבים מתקפלים, הצצה ליום, ציור בראש כל כרטיס
wallet.html + wallet.js   לינות, דדליינים, כרטיס כתובת ביפנית לנהג מונית
tasks.html + tasks.js     החלטות פתוחות + משימות לפי דחיפות
tools.html + tools.js     שעון יפן, מחשבון ין, מספרי חירום, ספר החותמות
documents.html + .js      מסמכים
sw.js     service worker · pwa.js רישום + חיווי אופליין
index.html → מפנה ל-today.html · site.html = האתר הישן
```

חמישה פריטים בניווט: היום · מסלול · ארנק · משימות · כלים.
‎start_url במניפסט מצביע על ‎today.html.

**מסך אחד, תוכן מתחלף.** כל שלד HTML טוען את **כל** קבצי המסכים, וכל אחד
מהם רושם את עצמו ב-‎`App.screen('wallet.html', fn)` במקום לרוץ בטעינה.
‎`App.boot()` רץ פעם אחת ב-`DOMContentLoaded`, מקים את הסצנה ובוחר מה לצייר
לפי הכתובת. מעבר לשונית (לחיצה, החלקה, כפתור חזרה) מצייר מחדש **רק ‎`#main`**
דרך ‎`App.goto`, ו-‎`history.pushState` שומר כתובת אמיתית — כך שקישור ישיר,
רענון ו-‎`?d=` ממשיכים לעבוד. מדוד: 33ms במעבר מול 84ms בטעינת מסמך.

שלוש מלכודות שנפלו בדרך, וכל אחת מהן תחזור למי שיוסיף מסך:
- **‎`#main` מוחלף באלמנט חדש בכל ציור**, לא רק מנוקה — מסכים תולים עליו
  מאזינים מוסמכים, ובלי החלפה הם מצטברים.
- **מה שחי מחוץ ל-‎`#main` צריך להיות יציב.** ‎`#hello` נכתב ב-`innerHTML`
  דרך ‎`App.greet` (קודם הוחלף ב-`outerHTML` ונעלם), ובורר היום נוצר בתוך
  כותרת מסך היום במקום להיות מועבר לשם מגוף העמוד.
- **טיימר או מאזין ברמת ‎window חייב ‎`App.onLeave(fn)`** שינקה אותו, אחרת
  כל ביקור חוזר משאיר עוד אחד.

**רק 25% מהפעילויות נושאות שעה, אבל 57% מהימים מכילים לפחות אחת.**
מסך היום מקדם את האחרונה שעברה כשיש שעות, ואומר "היום" ולא "עכשיו"
כשאין. נוסה כאן גם מסך "עכשיו" נפרד — ירד, כי הוא היה אוברקיל: מסך
היום כבר עונה על אותה שאלה בשורה הראשונה שלו.

- **ערכות:** יום 06:00–17:00, לילה אחרת. `?theme=day|night` כופה.
- **מזג אוויר:** Open-Meteo, בלי מפתח, נשמר לשעה. קובע חלקיקים (גשם/שלג/ערפל/עלים).
  `?wx=rain` כופה. **הבקשה רצה בדפדפן של המשתמש** — ‎api.open-meteo.com חסום
  מהסביבה המרוחקת, ולכן בודקים אותה עם ‎`page.route` שמזייף תשובה.
  השורה העליונה בכל מסך אומרת מה חזר: מצב, מעלות, איפה נמדד, ולפני כמה זמן.
  "נכפה" כשזה ‎?wx=, "אין נתון" כשהבקשה נפלה — אף פעם לא מספר מומצא.
  **הכל נופל באלכסון שמאלה**, כמו בקנבס של האתר: `--k` הוא היחס בין הסחיפה
  למרחק הנפילה, ולכן הזווית זהה בכל גובה מסך. לעלה יש שלוש שכבות — נפילה,
  נדנוד, סיבוב — כי שלושתן כותבות `transform` ועל אלמנט אחד הן דורסות זו את זו.
- **שער המטבע:** `App.fxRate(cb)` מושך מ-‎`api.frankfurter.app` פעם ב-20 שעות
  (מטמון ב-`localStorage`, בקשה אחת גם כששני קוראים שואלים במקביל). **השער
  ב-`data.js` הוא עוגן** — התקציב ו-`check.js` נשענים עליו והוא לא זז; רק
  מחשבון הכיס ב"כלים" עובר לשער החי. תשובה מחוץ לטווח שפוי או בקשה שנפלה
  נדחות והמחשבון נשאר על העוגן. גם הדומיין הזה חסום מהסביבה המרוחקת —
  בודקים עם `page.route`.
- **הוצאות:** `App.spend` ב-`app.js` — תור מקומי ב-`localStorage` שנשלח לגיליון
  Google דרך Apps Script (`apps-script.gs`, הוראות ב-`SHEETS.md`). **כתובת הקצה
  לא בריפו** — היא מודבקת פעם אחת בכל טלפון ונשמרת מקומית, כי הריפו ציבורי.
  לכל שורה `id` והצד השני מתעלם מ-id שכבר ראה, ולכן שליחה חוזרת אחרי אופליין
  לא מכפילה. השער נשמר עם השורה ולא מחושב בדיעבד. הבקשה היא `text/plain`
  בכוונה — Apps Script לא עונה ל-OPTIONS.
- **החלקה בין לשוניות:** `swipeNav` ב-`app.js` — אצבע שמחליקה מחליפה מסך
  לפי סדר הניווט (ב-RTL: שמאלה = הפריט הבא). שני ספים, כי בלעדיהם כל גלילה
  מהירה באלכסון מחליפה מסך: מרחק ≥70px **וגם** אופקי ≥1.6× מהאנכי. לא נתפס
  על `input`, `select`, `.jump` ו-`.tcard`, ואין גלגול מהקצה.
- **בחירת יום:** `?d=N`. לפני הטיול מוצגת ספירה לאחור ויום 1.
- `prefers-reduced-motion` מכבה את כל התנועה.

## הסצנה — ציורים, לא קוד

הנוף בראש כל מסך היה SVG פרוצדורלי. **עכשיו הוא 68 ציורים** ב-`assets/`
(WebP, שקופים, 1.8MB). `ASSETS.md` הוא המסמך המלא; הנה מה שחייבים לדעת:

- **שתי מערכות צירים.** `.l-village` הוא SVG עם `preserveAspectRatio="none"`
  שנמתח ×1.144 לרוחב. שכבות ה-div לא נמתחות. **תמונה לא נכנסת ל-SVG** —
  היא תתעוות. ההמרה: `left = x/3.9%` · `bottom = (74 − y) × AK`.
- **`AK` (כרגע 1.42)** מותח את גובה הבלוק המצויר. הקואורדינטות הפנימיות
  (74 = קו הקרקע, 104 = ה-viewBox) לא זזות. מוזרק כ-`--ak` כדי שה-CSS
  וה-JS יקראו מאותו מקום.
- **סדר השכבות:** `l-sky` → `l-far` (רכסים) → `l-back` (עצים) → `l-village`
  (קרקע, כביש, שורות עומק) → `l-house` → `l-art` (ציון דרך, רחוב) → `l-chars`.
- **הכל נגזר מזרע יומי** (`rng(dayIndex()+1)`): פריסת הבתים, הרכס, השמיים,
  מיקום ציון הדרך. אותו יום תמיד נראה אותו דבר.
- **קנה מידה לפי המימד הקצר.** זה הלקח שחזר בכל קבוצה: ברוחב קבוע חתולה
  בפורטרט יצאה כפולה בגובה, ודנגו גלש מהכרטיס. הרכס הוא היוצא מן הכלל —
  שם הרוחב קובע, כי הוא חייב לעבור את רוחב המסך.
- **תמונה לא משנה צבע עם הערכה.** ה-SVG שאב מ-`var(--cat1)`, תמונה לא.
  בלילה מוכהה ב-`filter` — חתולה/עץ/בית שמוארים כמו ביום נקראים כמדבקה.
- **ציוני הדרך הפוכים למה שהשם אומר:** `-day` הוא הצבעוני ו-`-night` הצללית
  הכהה. ביום משתמשים בצללית, בלילה בצבעוני.
- **ה-SVG הישן נשאר מתחת כגיבוי** ונעלם כשהתמונה נטענת — אין הבזק, ואם
  קובץ חסר נשאר משהו על המסך.

```bash
python3 assetize.py <תיקיית-PNG>   # → assets/ + manifest.json (דורש Pillow)
python3 dechecker.py <קובץ.png>    # מסיר דמקת שקיפות שנצרבה לפיקסלים
```

**כל קובץ חדש חייב להיכנס ל-`SHELL_FILES` ב-`sw.js`**, אחרת הסצנה ריקה אופליין.

## אחרי כל שינוי ב-`data.js` / CSS / JS

```bash
./bump.sh
```

מעלה את `?v=` בכל ה-HTML, מעלה את `V` ב-`sw.js` (מוחק קאשים ישנים), מריץ
`build-trip-json.js` (מייצר `trip.json`), ואז `check.js` שמאמת עקביות —
ספירת לילות, שיוך שלבים, סכומי תקציב, טריות שערי מטבע. **אם `check.js` נופל, משהו נשבר.**

## מלכודות

- **`styles.css` ו-`app.css` חיים יחד.** `styles.css` הוא של האתר הישן ומוגש גם
  בדפים החדשים דרך `site.html`. היו כבר התנגשויות שמות מחלקות (`.bar`). לבדוק לפני
  שמוסיפים שם גנרי.
- **נתיבים יחסיים בלבד.** Pages מגיש מתת-תיקייה (`/nipon26/`), לא משורש הדומיין.
  `/sw.js` ישבור הכל; `sw.js` עובד.
- **`trip.json` נבנה, לא נערך.** עורכים `data.js`.
- **הריפו ציבורי** ומכיל כתובות מלונות, סכומים ולינקים למסמכי הזמנה. לפני שמוסיפים
  יומן או משהו אישי — לסגור אותו.
- **`drive.google.com` חסום מהסביבה המרוחקת.** למשוך קבצים רק דרך מחבר הדרייב;
  התוצאה נשמרת לקובץ ולא לקונטקסט, ומפענחים אותה עם `base64` ב-python.
- **גובה הסצנה הוא `--sceneH`,** משתנה אחד ש-‎`.scene` וכל מי שנשען עליו קוראים.
- **`.scene` הוא `fixed` והתוכן נגלל מעליו.** במסכים שמתחילים בכותרת היא מקבלת
  `margin-top` שמוריד את התוכן מתחת לסצנה — אחרת הכרטיס הראשון מכסה את קומת הרחוב.

## איך לעבוד כאן

יובל אמר את זה מפורשות, אחרי סשן שבו זה לא קרה:

1. **צעד אחד בכל פעם, עם תוצר נראה לעין בסופו.** לא שישה מסכים לפני שבודקים כיוון.
2. **לרנדר לפני שמוסרים.** יש Playwright וכרומיום. אם מסרת UI בלי להסתכל עליו — טעית.
3. **לא לתקן באגים שלא ביקשו.** במיוחד לא באמצע משימה אחרת.
4. **לא להמציא עובדות.** אם אינך בטוח ביכולת או בפרט טכני — לבדוק, ואז לומר.
5. **לא לחזור על החלטות סגורות.** `APP-PRD.md` מחזיק אותן.
6. **לא לשאול כשאפשר פשוט לעשות.** אם הוא הביא קובץ — להכניס אותו.

## מסמכים

- `FOR-YUVAL.md` — **מה מחכה ליובל: דדליינים, שאלות תוכן, ומה חוסם המשך**
- `REVIEW.md` — **ביקורת 16.9.2026: מה נמצא, מה תוקן, ומה נשאר פתוח**
- `SHEETS.md` — גיליון ההוצאות: התקנה, ומה קורה אופליין
- `ASSETS.md` — **שבע קבוצות הציורים, מה נלמד בכל אחת, ומה עוד לא מחובר**
- `APP-PRD.md` — החלטות האפליקציה: למה PWA ולא APK, המסכים, הפיצ׳רים והמה שנחתך
- `METHOD.md` — איך נבנה המסלול והמוסכמות של `data.js`
- `MANIFEST.md` — עובדות הטיול
- `mockup/` — מסכי עיצוב (`.dc.html`). הכיוון הוויזואלי: אנימה, אדו, שתי ערכות.
