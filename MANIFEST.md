# NIPON26 — מפת הפרוייקט (קרא תחילה)

> מטרה: כל סשן של קלוד מתחיל "קר". הקובץ הזה = המפה. קרא אותו ואת `data.js` ואתה יודע הכל — בלי לחפש בכל הדרייב.

## הטיול
יפן 2026, יובל & שיר. 14 אוק' – 25 נוב' 2026 (~42 לילות). מרדף שלכת צפון→דרום. כניסה/יציאה טוקיו.

## מבנה האתר (GitHub Pages: https://dfarchi.github.io/nipon26/)
| קובץ | תפקיד |
|------|--------|
| `index.html` | שער: עובדות סגורות + ניווט |
| `decisions.html` | ההחלטות האינטראקטיביות (סימון + ייצוא טקסט) — המספר נגזר מ-`data.js`, לא כתוב קשיח |
| `itinerary.html` | רשימת השלבים (נגזרת מ-`data.js`) — לחיצה פותחת עמוד שלב |
| `stage.html` | עמוד שלב (`?s=N`): מפה + לינקים + יום-ביום של אותו שלב |
| `transit.html` | תחבורה: למה JR Pass לא משתלם (חישוב מלא), כרטיסי IC, כרטיסיות טוקיו |
| `documents.html` | קישורים למסמכי הדרייב |
| `data.js` | **מקור הנתונים** — שלבים, החלטות, ו-ID של כל מסמך דרייב |
| `mapexport.js` | בונה KML מ-`TRIP.mapPoints` (שכבה לכל שלב) לייבוא ל-Google My Maps |
| `basemap.js` | שכבת רקע משותפת לשתי המפות — CARTO (תוויות באנגלית). להחלפת סגנון: שדה `style` |
| `styles.css` | עיצוב משותף |
| `check.js` | בדיקת שפיות — `node check.js` לפני כל push |
| `bump.sh` | מעלה את מחרוזת המטמון בכל ה-HTML ומריץ את הבדיקה |

## אין מספרים כתובים קשיח ב-HTML
כל ספירה שמופיעה באתר (לילות, שלבים, החלטות פתוחות, לילות מוזמנים) נגזרת מ-`data.js` בזמן ריצה.
אם צריך להוסיף מספר לעמוד — לחשב אותו מ-`TRIP`, לא להקליד. מספרים מוקלדים מתיישנים בשקט.

## מטמון (cache) — כלל קבוע
כל קריאה לקובץ מקומי ב-HTML נושאת `?v=YYYYMMDDx` (למשל `data.js?v=20260817a`).
GitHub Pages מגיש JS/CSS עם מטמון של 10 דקות, ובלי זה שינויים לא נראים בדפדפן.
**אחרי כל שינוי ב-`data.js` / `basemap.js` / `styles.css` / `delight.js` / `mapexport.js` —
הרץ `./bump.sh`** (מעלה את המחרוזת בכל ה-HTML ומריץ את `check.js`), אחרת יובל יראה גרסה ישנה.

## מסמכי הדרייב (מוגבלים ליובל & שיר בלבד)
ID-ים מלאים שמורים ב-`data.js` תחת `TRIP.docs`. לעדכון: קפוץ ישירות ל-ID, אל תחפש בכל הדרייב.
- Trip Brief (עובדות סגורות) · החלטות לסמן · הצפון · גיליון מעקב (Sheet) · Flight Details

## זרימת העבודה
1. מקור האמת = מסמכי הדרייב.
2. יובל מסמן בחירות ב-`decisions.html` → "העתק בחירות" → שולח לקלוד.
3. קלוד מעדכן `data.js` (שדה `decided`) ו/או את המסמכים בדרייב, ודוחף.
4. **כלל קבוע:** כשיובל מאשר יעד לינה סופי (מלון/ריוקאן/אונסן) — קלוד מוסיף אוטומטית ל-`data.js` שני מקטעי מסלול: (א) הגעה מהתחנה/שדה התעופה הקודם ליעד, (ב) יציאה מהיעד לתחנה/רכבת הבאה. משתמשים ב-`dir(a,b)` (לינק ניווט תחבורה ציבורית חי ב-Google Maps) עם שם המקום, לא הכתובת המלאה.
5. **כלל קבוע:** אם קיים לינק ישיר להזמנה (Booking.com וכו') — הוא עדיף על פני קישור למסמך ב-Drive. אם אין לינק ישיר, ממשיכים עם מסמך הדרייב עד שיסופק.

## Git — עבודה ישירה על main
**קבוע, לא לשאול שוב:** לעבוד ולדחוף ישירות ל-`main`. לא לפתוח ענפי פיצ'ר מקומיים אלא אם מתבקש מפורשות.

## העדפות תכנון קבועות (יובל)
- **אין צורך למלא ימים בכוח.** הטיול ~42 לילות. פעילות שלא נכנסה ביום מסוים לא "אבדה" — תמיד אפשר לעשות משהו אחר בהמשך. ימים פתוחים הם תכנון תקין, לא חור.
- **הליכות: שטוחות בלבד, עד ~15 ק"מ, בלי טיפוסים ובלי תרמילים.** כל מסלול עם עלייה משמעותית מסומן כאופציונלי במפורש.
- **קעקועים** — חדר עם אמבט פתוח פרטי (露天風呂付き客室) או **貸切風呂** (אמבט פרטי להשכרה). השני זול בהרבה ופותר אותה בעיה.
- **הזמנות: מייל ואונליין בלבד. אין הזמנות טלפוניות.**
- **פינוק עולה כסף רק איפה שיש זמן ליהנות ממנו.** בסיס עירוני (קאנאזאווה/טקיאמה) או לילת מעבר (היראיו) = מיטה ומיקום, לא חוויה.
- **תקציב לינה: ~₪300–500 ללילה** (≈¥13,000–21,000 לחדר). מיטה רגילה מועדפת על פוטון/טאטאמי.
- **פורמט תשובות:** טבלאות, מספרים, לינקים. בלי הרבה טקסט.

## חומרים חיצוניים מוגני זכויות יוצרים
מדריכים שנקנו/הורדו (PDF של יוצרים עצמאיים וכו') **לא נכנסים לריפו** — הוא פומבי.
מותר לאמת את העובדות שבהם מול המקורות הרשמיים, לכתוב מחדש בעברית ולתת קרדיט ליוצר.
הקובץ המקורי נשאר בדרייב בלבד.

## ביטחון
הריפו **פומבי**. אסור להטמיע כאן: קודי הזמנה/PNR, מספרי כרטיסים, כתובות, טלפונים. מידע רגיש נשאר רק במסמכי הדרייב המוגבלים.
