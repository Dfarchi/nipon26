# הנכסים המצוירים — מה יש ואיך מכניסים אותם

48 קבצים שיובל ייצר, נמשכו מהדרייב ויושבים ב-`assets/`. WebP, שקופים,
חתוכים לגבולות האמיתיים, 1.2MB בסך הכל. `assets/manifest.json` מחזיק
רוחב/גובה/משקל לכל אחד — **נבנה, לא נערך**.

להוסיף או להחליף קובץ: לשים את ה-PNG הגולמי בתיקייה כלשהי ולהריץ
`python3 assetize.py <התיקייה>` (דורש Pillow), ואז `./bump.sh`. הקבוצה
והגודל נגזרים מהשם.

**כל שבע הקבוצות מחוברות.** ה-SVG הפרוצדורלי נשאר מתחת כגיבוי ונעלם
כשהתמונה נטענת — בלי הבזק בטעינה ראשונה, ועם משהו על המסך אם קובץ חסר.

לקח שחזר בכל שלב אחד: **קנה מידה לפי המימד הקצר, לא הארוך.** ברוחב
קבוע חתולה בפורטרט יצאה כפולה בגובה, דנגו גלש מהכרטיס, והרכס מילא את
השכבה. ברכס דווקא הפוך — שם הרוחב הוא הקובע, כי הוא חייב לעבור את
רוחב המסך.

---

## הכלל שמעליו הכל נשען

הסצנה חיה בשתי מערכות צירים, וזה מקור כל הבאגים כאן:

| שכבה | מה זה | מתיחה |
|---|---|---|
| `.l-village` | SVG, `viewBox="0 0 390 104"`, `preserveAspectRatio="none"` | **נמתח** ×1.144 לרוחב |
| `.l-chars` | div עם ילדים ממוקמים | **לא נמתח** |

**תמונה רסטר לא נכנסת ל-`.l-village`** — המתיחה תעוות אותה. כל נכס נכנס
לשכבת div משלו, כמו החתולות.

ההמרה בין השתיים, כשרוצים למקם נכס על משהו שנוצר ב-SVG:

```
left = x / 3.9 %          bottom = 74 − y  (פיקסלים)
```

מקור: השכבה יושבת ב-`bottom:30` וגובהה 74, והיא מורחבת ב-28px לכל צד
כמו ה-SVG (`right:-28;left:-28`), כך שהאחוזים מתיישרים.

---

## סדר העבודה

### ✅ שלב 0 — המלאי
נמשך מהדרייב, עובד, בריפו. `assetize.py` מייצר מחדש מכל תיקיית PNG.

### ✅ שלב 1 — ציוני דרך  `assets/landmarks/`
עשרה קבצים, יום ולילה לכל אחד: torii, pagoda, tokyo-tower, tsutenkaku,
nagoya-castle. יחס אחד־לאחד ל-`LM` ב-`app.js`.

- שכבה חדשה `.l-lm` בין `.l-village` ל-`.l-chars`, עומק פארלקס 0.42 (כמו הכפר)
- גובה לכל ציון דרך בטבלה, לא אחיד: מגדל טוקיו מתנשא, תורי נמוך
- **הקבצים הפוכים למה שהשם אומר.** ‎`-day` הוא הצבעוני ו-‎`-night` הוא
  הצללית הכהה — וצללית כהה נעלמת על שמי לילה. בפועל: ביום הצללית,
  בלילה הצבעוני. זה גם נכון למציאות (מגדל טוקיו והפגודה מוארים בלילה).
  נבדק ברינדור, הפגודה והתורי הכהים פשוט לא נראו בלילה.
- `theme` בוחר את הקובץ; אין החלפה חיה כי הסצנה נבנית פעם אחת
- ה-SVG הישן נשאר מתחת כגיבוי ונעלם כשהתמונה נטענת — כך אין הבזק,
  ואם הקובץ לא זמין עדיין נשאר משהו על המסך

### ✅ שלב 2 — פריטי רחוב  `assets/street/`
`vending-machine`, `toro`, `noren`, `chochin`, `utility-pole`, `water-tank`,
`red-umbrella`, `black-pine`, `maple-tree`, `gassho-farmhouse`, `chimney-smoke`.

מחליפים את הפונקציות ב-`app.js` (`vending`, `toro`, `norenAt`, `pole`,
`tank`, `treeAt`). אותה שכבה, אותה הגרלה יומית — רק שהצורה מגיעה מקובץ.
`gassho-farmhouse` מחליף את המשולש בפרופיל ההררי.

### ✅ שלב 3 — שמיים  `assets/sky/`
`sun`, `moon`, `clouds-day`, `clouds-night`, `birds`, `leaf-1..5`.

- שמש/ירח מחליפים את ה-orb ב-`.l-sky`
- העננים מרוצפים לרוחב ונעים לאט (הם כבר `.drift`)
- **חמשת העלים מחליפים את ה-`LEAF` הווקטורי בחלקיקים** — כל עלה נופל
  בצבע אחר במקום אותו נתיב עם `fill` מתחלף

### ✅ שלב 4 — רכסים  `assets/special/`
`ridges-day` / `ridges-night`, 1280 רוחב — בדיוק רוחב המסך ב-×3.

מחליפים את `farSVG()` (הרכס הפרוצדורלי). **הווריאציה היומית נשמרת**:
שני עותקים של אותו קובץ, ברוחב, בהזזה ובגובה שנגזרים מהזרע — הקרוב כהה
וגדול, הרחוק בהיר וקטן.

**שני דברים שנלמדו כאן:**
- קנה מידה לפי רוחב ולא לפי גובה. לפי גובה הרכס יצא צר מ-390px ונפער
  פס ריק בצד המסך.
- לתמונה יש קצה עליון חתוך ישר, וזה נקרא כתפר בין שמיים להרים. מסכה
  שממיסה את החלק העליון פותרת את זה.

**`ridges-day.png` ו-`ridges-night.png` הגיעו עם דמקת שקיפות צרובה
לתוך הפיקסלים** — הגנרטור צייר את משבצות הרקע במקום לא לצייר כלום.
`dechecker.py` מזהה את הגל הריבועי (היסט חצי מחזור שונה, היסט מלא זהה)
ומוחק את האזור לשקיפות. שווה לייצר אותם מחדש נקי אם אפשר.

### ✅ שלב 5 — חתולות  `assets/cats/`
`morgana-sit`, `bellatrix-sit`, `cat-sleep`, `cat-tail-up`.

**הזהירות הגדולה ביותר.** ה-SVG משנה צבע בין יום ללילה; תמונה לא.
פתרון: שכבת `filter: brightness()` בלילה. לבדוק לפני שמוחקים את
`A.cat` — אם זה נראה מודבק, נשארים על הווקטור בסצנה ומשתמשים
בתמונות רק בכרטיסים הגדולים.

### ✅ שלב 6 — המנה היומית  `assets/food/`
`ramen`, `gyutan`, `takoyaki`, `onigiri`, `nigiri-salmon`, `matcha`,
`dango`, `yaki-imo`.

פיצ׳ר חדש, לא החלפה. כרטיס במסך "היום": מה אוכלים באזור הזה היום.
השיוך אזור→מנה יושב ב-`data.js` (גיוטן בסנדאי, טקויאקי באוסקה,
מאצ׳ה בקיוטו). בגודל 288px הן נראות מצוין.

### ✅ שלב 7 — חותמות  `assets/shuin/`
שש מסגרות 朱印 אדומות.

חותמת לכל אזור שישנתם בו — שמונה אזורים, שש מסגרות (חוזרות, כמו
בספר אמיתי). יושב ב-`tools.html`.

**הזכייה נגזרת מהתאריך ולא נשמרת בשום מקום.** אין `localStorage`: מה
שעבר עבר. אין מה לאבד כשמוחקים נתוני דפדפן, אין מה לזייף, ואין מצב
שצריך לתחזק. לפני הטיול כל השמונה חיוורות — וזו תצוגה מקדימה נחמדה
של מה שמחכה.

המסגרות לא סימטריות אנכית, ולכן הטקסט זז לפי צורה: במשולש (קויאסאן)
הוא יורד לחלק הרחב ומוקטן.

---

## כללים לכל שלב

1. **ה-service worker חייב לקבל את הקבצים.** `SHELL_FILES` ב-`sw.js`
   נבנה מ-`assets/manifest.json`, אחרת הסצנה ריקה אופליין.
2. **`./` ולא `/`.** Pages מגיש מתת-תיקייה.
3. **טעינה בלי הבזק.** הנכס נכנס ב-`opacity:0` ועולה ב-`load`.
4. **לרנדר לפני שמוסרים.** יום ולילה, ואת האזור שהנכס שייך לו.
5. **הישן נמחק רק אחרי שהחדש נראה טוב** — לא באותו commit.


---

## ציורים שלמים  `assets/vignettes/`

ארבעת הקבצים שהגיעו כציור מלא לפני שביקשנו אלמנטים בודדים. הם לא
מתאימים לסצנה — הם מביאים רקע משלהם — אבל יובל אהב אותם, וראש כרטיס
שלב הוא בדיוק המקום לתמונה שעומדת בפני עצמה.

יושבים בפינה השמאלית של כרטיס השלב ב-`itinerary.js`, ב-20–26% אטימות
ועם מסכה רדיאלית. **בצד שמאל**: ב-RTL הטקסט מתחיל מימין, ושם הציור
התחרה בכותרת.
