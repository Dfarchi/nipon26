# NOTES — איפה הדברים עומדים

עודכן: 12.9.2026 · ענף `App` · חי בכתובת https://dfarchi.github.io/nipon26/

מסמך המשכיות: מה נבנה, מה לא, ואיפה להמשיך. `APP-PRD.md` הוא עדיין מקור ההחלטות.

---

## איך זה מגיע לטלפון (השאלה שחזרה כמה פעמים)

```
עריכה מקומית → ./bump.sh → git commit → git push origin App
   → GitHub Pages בונה (~דקה) → הטלפון מקבל בפתיחה הבאה עם רשת
```

שלוש נקודות שהכשילו את זה בפועל, שכדאי לזכור:

1. **`git push` הוא חלק מהתהליך, לא תוספת.** commit מקומי לא מגיע לאף טלפון.
2. **חייבים לקמט את `sw.js` יחד עם ה-HTML.** `bump.sh` מעדכן את `V` ב-`sw.js`,
   וזה הבייט היחיד שגורם לדפדפן לזהות שיש גרסה חדשה. פעם אחת עשיתי
   `git add` רק ל-`*.html` — הגרסה "עלתה" אבל שום טלפון לא זיהה כלום.
   **לוודא ש-`sw.js` נכלל בכל commit שמגיע אחרי `bump.sh`.**
3. **עדכון ראשון אחרי שינוי ב-`pwa.js` הוא שקט.** באנר "גרסה חדשה" מוצג
   על ידי הקוד שכבר טעון בדפדפן, אז שינוי בבאנר עצמו מתגלה רק בפעם שאחריה.

---

## מה נבנה עד עכשיו

**ארבעה מסכים באפליקציה** (`app.css` + `app.js` + סקריפט לכל מסך):

| מסך | קובץ | מצב |
|---|---|---|
| היום | `today.html` / `today.js` | פעיל · כולל בחירת יום `?d=N` |
| מסלול | `itinerary.html` / `itinerary.js` | פעיל · שורות הימים לחיצות → `today.html?d=N` |
| ארנק | `wallet.html` / `wallet.js` | פעיל · כרטיס נהג, דדליינים, ייצוא `.ics` |
| מה פתוח | `tasks.html` / `tasks.js` | פעיל · סימון החלטות ומשימות במקום |
| מסמכים | `documents.html` / `documents.js` | הומר לקליפה החדשה (לא בניווט התחתון) |

**שכבת האיור (`art.js`)** — הכל מקורי, מצויר ביד ב-SVG. אין שם שום דמות מוגנת:
זו הייתה מגבלה מפורשת ב-PRD §7 ("palette, composition and energy are fair game"),
אז יש חתולות משלנו ולא דמות מסדרה. הצבעים באים ממשתני CSS, כך שהאיור מתחלף
עם הערכה ולא צריך שתי גרסאות לכל ציור.

| מה | מתי מופיע |
|---|---|
| שתי חתולות על הגגות | תמיד. ביום ערות, זנב מתנדנד ועיניים ממצמצות; בלילה ובקור מכורבלות וישנות |
| נינג׳ה על הגג הימני | לילה בלבד — צללית, סרט ראש מתנופף |
| מטייל עם כובע קש | יום בלבד — חוצה את המסך לאט |
| פנסי נייר על חוט מאצורי | לילה — תלויים על קשת בזייה, מתנדנדים |
| ירח + כוכבים מנצנצים | לילה |
| שמש עם הילה, עננים נודדים, ציפורים | יום |
| מטרייה מעל החתולה | גשם |
| כובע שלג על החתולה | שלג |
| אובך על התחתית | ערפל |
| אוניגירי | מצב "יום פנוי" ב"היום המלא" |
| סושי, טורי | ב-`art.js`, עדיין לא בשימוש — מוכנים |

בגשם/שלג/ערפל השמש והירח מתעמעמים והציפורים נעלמות, אחרת שמש בוהקת
באמצע גשם נראית כמו באג. הכל מכובה תחת `prefers-reduced-motion`.

**תשתית:**
- באנר "🔄 גרסה חדשה זמינה — הקישו לרענון" (`pwa.js`).
- פס אופליין שמראה **מתי סונכרן לאחרונה**, לא רק "אין רשת".
- `apple-touch-icon` בכל הדפים — בשביל אייקון תקין במסך הבית באייפון.
- `TRIP.stayAddr` ב-`data.js`: הצטרפות מפורשת בין כל הזמנה ב-`booked`
  לכתובת ב-`HOTEL`. `check.js` נופל אם הזמנה לא מכוסה.

**ניווט — מהמדויק לכללי** (`today.js`):
1. לינק "🧭 מסלול" של פעילות מהיום → 2. כתובת הלינה של הלילה (`stayAddr`)
→ 3. שם העיר בלטינית/יפנית → 4. **אין יעד מדויק? אין כפתור.**
עדיף בלי כפתור מאשר לשלוח לחיפוש "טוקיו" כללי כשעומדים ברחוב.
ימים 40 ו-44 (סוף טוקיו) בלי כפתור — כי המלון שם עוד לא הוזמן.
**ברגע שההזמנה תיכנס ל-`booked` ול-`STAY_ADDR`, הכפתור יחזור לבד.**

---

## מה עוד לא ממומש

### מתוך ה-MVP ב-PRD
- **QR בארנק.** ה-PRD מזכיר QR, אבל ב-`data.js` אין קודי הזמנה להצפין.
  צריך להחליט **מה** מקודדים לפני שבונים.
- **יומן.** לא נבנה בכלל. לפי ה-PRD: נכתב בשיחה עם Claude, נוחת בריפו כקבצים,
  מוצג בדפי הימים.

### פערים ידועים (PRD §6) — עדיין פתוחים
- **המצב הוא לפי מכשיר.** סימוני משימות והחלטות ב-`localStorage`.
  יובל מסמן — שיר לא רואה. דורש backend קטן / Firebase.
- **עריכה אופליין** לא מסתנכרנת אחר כך.
- **הריפו פומבי** ומכיל כתובות מלונות וסכומים. להחליט לפני שנכנס יומן.

### Nice to have (PRD §5) — לא נגענו
Share-target ממפות · מעקב הוצאות · דפדפן מקומות שמורים.

### עיצוב (PRD §7) — עדיין פתוח
הסקין הוא סיבוב ראשון. השאלות הפתוחות לא נענו: אילו אנימות, תמונות של
מורגנה ובלטריקס, ואריגי ukiyo-e כשכבת טקסטורה. גם "שזה יזרום" —
view transitions, spring easing, skeletons, ג׳סטות — לא נבנה.

### במכוון לא הומר
`budget.html` · `decisions.html` · `transit.html` · `stage.html` נשארו
בסקין האתר הישן. הם תוכן **תכנון**, ולא חלק מרשימת המסכים של האפליקציה
ב-`APP-PRD.md`. `decisions.html` עדיין מקושר מ"מה פתוח" ככלי עריכה עמוק
(הערות, פילטרים) — הפעולה היומיומית (לסמן) כבר עובדת בתוך האפליקציה.

---

## דברים שכדאי לדעת לפני שממשיכים

- **הבאנר "היי שיר 👋" ב-`today.html` הוא זמני** — נועד לבדוק שעדכונים
  מגיעים למכשיר. להוריד ברגע שמאשרים. הוא לא תוכן אמיתי.
- `check.js` הוא רשת הביטחון. אם הוא נופל, לא לדחוף.
- אין Playwright ואין תוסף דפדפן בסביבה הזו. הרינדור נעשה עם
  Chrome headless ישירות:
  `chrome --headless=new --screenshot=out.png --window-size=430,900 URL`
  אפשר לאמת יעדי ניווט עם `--dump-dom` וגרפ על `location.href`.

---

## תוכנית: שכבת ההנאה (נכתב 14.9.2026)

יובל ביקש ארבעה דברים ואמר במפורש **לא לממש הכל בבת אחת**. זו התוכנית,
לפי סדר "כמה זה מוסיף חלקי כמה זה מפריע".

**החומר כבר קיים ב-`delight.js`** — 14 מנות, 12 ציטוטי חתולות, 4 חותמות,
עלים נופלים. אבל הוא נטען רק בדפי האתר הישן (`budget`, `decisions`,
`site`, `stage`, `transit`) ולא באפליקציה, והמינון שלו שגוי: ציטוט כל
14–24 שניות. זה מה שיובל קרא לו "חופר".

### 1 · המנה היומית — הכי שימושי, הכי זול

היום: `DISHES[doy % 14]` — יום בשנה מודולו 14. כלומר המנה לא קשורה לשום
דבר. **אבל `data.js` יודע באיזו עיר אתם באותו יום**, וזה הופך קישוט למידע:
גיוטאן בסנדאי, טאקויאקי באוסקה, מאצ׳ה בקיוטו, בטטה צלויה בהרים בסתיו.

- לבנות: מיפוי מנה→אזור, ונפילה למנה כללית כשאין התאמה.
- מקום: שורה במסך "היום", מתחת להערת היום.
- **שאלה פתוחה:** מנה אחת ליום, או "מה לאכול כאן" כרשימה קצרה?

### 2 · החתולות — הכי אופייני, והכי קל להרוס

מורגנה ובלטריקס כבר מצוירות על הגגות ב-`art.js`. הציטוטים קיימים.
מה שחסר הוא **מתי**.

- **לא** טיימר. ציטוט אקראי כל 20 שניות הוא מה שהיה, וזה נמאס אחרי יומיים.
- כן: אירוע. הגעה לעיר חדשה · שלב שנסגר · משימה שסומנה · דדליין שעבר ·
  הפעם הראשונה ביום שפותחים את האפליקציה.
- תקרה קשיחה: **לכל היותר פעם אחת בפתיחה, ולא בכל פתיחה.**
- **שאלה פתוחה:** האם לכתוב ציטוטים חדשים שקשורים למקום (מורגנה על
  קויאסאן, בלטריקס על האונסן), או להסתפק ב-12 הקיימים שהם כלליים?

### 3 · חותמות — הגיימיפיקציה, ויש לה צורה יפנית מוכנה

היום: 4 חותמות על ביקור בדפים. זה מודד גלישה, לא טיול.

הצורה הנכונה היא **御朱印 / חותמות תחנה** — מסורת אמיתית ביפן, ומתאימה
בול לכיוון ההדפס. חותמת לכל עיר, לכל שלב שנסגר, לכל אונסן.

- לבנות: מסך ספר חותמות, וחותמות שנגזרות מ-`data.js` (ערים, שלבים).
- **שאלה פתוחה, והיא המרכזית:** חותמת נפתחת **אוטומטית** לפי התאריך
  (כן תהיו שם, אז זה כנה), או **בצ׳ק-אין ידני** (יותר משחק, אבל אפשר
  לרמות ולכן זה שווה פחות)? זו השאלה ששווה לענות עליה לפני שבונים.
- ⚠️ המצב הוא פר-מכשיר. יובל יאסוף חותמות ושיר לא תראה אותן — אלא אם
  זה נפתר קודם (ראו "פערים ידועים").

### 4 · מסך פתיחה עם שלכת — הכי יפה, והכי מסוכן

`particles()` ו-`art.js` כבר יודעים לצייר עלים נופלים; אין צורך בנכס חדש.

⚠️ **אבל זו האפליקציה שנפתחת על רציף ברכבת כדי לדעת מה עכשיו.** מסך פתיחה
מוסיף שנייה לכל פתיחה, ובדיוק ברגע שבו אין שנייה.

אם בכל זאת:
- רק ב**הפעלה קרה** (standalone), לא במעבר בין מסכים.
- 900ms לכל היותר, ולחיצה מדלגת.
- כבוי תחת `prefers-reduced-motion`.
- iOS לא מאפשר splash מונפש נייטיב (`apple-touch-startup-image` הוא תמונה
  סטטית), אז זו שכבה בתוך האפליקציה — כלומר היא באמת מעכבת.
- **הצעה חלופית:** במקום מסך פתיחה, להעצים את השלכת בסצנה עצמה באוקטובר–נובמבר.
  אותו רגש, אפס עיכוב.

### סדר מומלץ

1 (מנה) ← 3 (חותמות) ← 2 (חתולות) ← 4 (פתיחה, ורק אם עדיין רוצים).
המנה מוסיפה ערך אמיתי, החותמות הן המבנה, החתולות הן התבלין, והפתיחה
היא היחידה שעלולה לגרוע.
